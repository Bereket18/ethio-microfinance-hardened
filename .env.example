# Copy this file to .env and fill in real values.
# .env is gitignored -- never commit real credentials.

# --- Database ---
DB_HOST=db
DB_PORT=3306
DB_NAME=ethio_microfinance
DB_USER=ethiomf_app
DB_PASS=CHANGE_ME_TO_A_STRONG_PASSWORD
DB_ROOT_PASS=CHANGE_ME_TO_A_DIFFERENT_STRONG_PASSWORD

# --- App ---
# Published host port -- what you'll browse to (http://localhost:APP_PORT)
APP_PORT=8080


# Copy this file to .env and fill in real values.
# .env is gitignored -- never commit real credentials.

# --- Database ---
DB_HOST=db
DB_PORT=3306
DB_NAME=ethio_microfinance
DB_USER=ethiomf_app
DB_PASS=Bereket@Asress7548
DB_ROOT_PASS=Bereket@1881

# --- App ---
# Published host port -- what you'll browse to (http://localhost:APP_PORT)
APP_PORT=8080
APP_TLS_PORT=8443